{% trans_default_domain 'auth-google.status' %}

{% if active %}
    <div class="badge text-bg-success">
        {{ 'active'|trans() }}
    </div>

{% else %}
    <div class="badge text-bg-danger">
        {{ 'block'|trans() }}
    </div>
{% endif %}